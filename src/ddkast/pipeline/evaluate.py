from __future__ import annotations

from ddkast.config import Config


def run(config: Config) -> None:
    """Compute metrics for the forecast and the baseline, and print a report."""
    raise NotImplementedError
